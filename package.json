{
  "name": "little-shutdown-program",
  "version": "0.0.1",
  "url": "https://github.com/undefinedCoding/little-shutdown-program/issues",
  "description": "A little program, that turns off the computer if the timer is done",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/undefinedCoding/little-shutdown-program.git"
  },
  "main": "main.js",
  "scripts": {
    "start": "electron ."
  },
  "dependencies": {
    "cross-spawn": "^6.0.5",
    "electron-shutdown-command": "0.0.8"
  },
  "devDependencies": {
    "electron": "^1.8.3"
  }
}
